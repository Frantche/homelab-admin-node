#cloud-config
groups:
  - homelab

users:
  - name: admin
    groups: [wheel, homelab]
    shell: /bin/bash
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    ssh_authorized_keys:
      - "ssh-ed25519 AAAA_PLACEHOLDER_REPLACE_ME admin@example"

package_update: true
packages:
  - ansible
  - git
  - docker
  - docker-compose
  - go
  - openssh
  - rsync
  - sops
  - age
  - restic
  - jq
  - curl
  - python
  - sudo
  - nftables
  - fail2ban
  - audit
  - lynis

write_files:
  - path: /etc/admin-node/mode
    owner: root:root
    permissions: '0644'
    content: |
      locked
  - path: /usr/local/bin/admin-node-converge
    owner: root:root
    permissions: '0755'
    content: |
      #!/usr/bin/env bash
      set -euo pipefail
      REPO_DIR="${REPO_DIR:-/opt/homelab-admin-node}"
      BUILD_PATH="$REPO_DIR/scripts/build-admin-node.sh"
      BIN_PATH="$REPO_DIR/bin/admin-node"
      if [[ -x "$BUILD_PATH" ]]; then
        "$BUILD_PATH"
      fi
      if [[ -x "$BIN_PATH" ]]; then
        exec "$BIN_PATH" converge run
      fi
      echo "[admin-converge] $BIN_PATH not found"
      echo "[admin-converge] initial clone should be done by cloud-init into $REPO_DIR"
      exit 1
  - path: /etc/systemd/system/admin-converge.service
    owner: root:root
    permissions: '0644'
    content: |
      [Unit]
      Description=Admin node ansible converge
      After=network-online.target
      Wants=network-online.target

      [Service]
      Type=oneshot
      ExecStart=/usr/local/bin/admin-node-converge
  - path: /etc/systemd/system/admin-converge.timer
    owner: root:root
    permissions: '0644'
    content: |
      [Unit]
      Description=Periodic admin converge

      [Timer]
      OnBootSec=5min
      OnUnitActiveSec=30min
      Unit=admin-converge.service

      [Install]
      WantedBy=timers.target

runcmd:
  - [ mkdir, -p, /etc/admin-node ]
  - [ bash, -lc, 'REPO_URL="${REPO_URL:-https://github.com/Frantche/homelab-admin-node.git}"; if [ ! -d /opt/homelab-admin-node/.git ]; then git clone "$REPO_URL" /opt/homelab-admin-node || { echo "[cloud-init] initial clone failed for /opt/homelab-admin-node using $REPO_URL" >&2; echo "[cloud-init] verify repository access/authentication and network connectivity" >&2; exit 1; }; fi' ]
  - [ systemctl, daemon-reload ]
  - [ systemctl, enable, --now, sshd ]
  - [ systemctl, enable, --now, docker ]
  - [ systemctl, enable, --now, admin-converge.timer ]
