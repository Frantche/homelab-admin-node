http:
  services:
    keycloak:
      loadBalancer:
        servers:
          - url: "http://keycloak:8080"
    openbao:
      loadBalancer:
        servers:
          - url: "http://openbao:8200"
    harbor:
      loadBalancer:
        servers:
          - url: "http://harbor-core:8080"
