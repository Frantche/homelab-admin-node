services:
  harbor:
    image: goharbor/harbor-core:v2.12.0
    container_name: harbor
    restart: unless-stopped
    environment:
      HARBOR_ADMIN_PASSWORD: ${HARBOR_ADMIN_PASSWORD}
    ports:
      - "8082:8080"
    volumes:
      - /srv/admin/data/harbor:/data
