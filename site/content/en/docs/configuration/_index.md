---
title: Configuration
weight: 30
---

Configuration is split between repository defaults and the private config repo.

Repository defaults live in `ansible/group_vars/all.yml`. Override deployment-specific values in the private config repo.
