---
- name: Ensure /etc/docker exists
  ansible.builtin.file:
    path: /etc/docker
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Configure docker log rotation
  ansible.builtin.template:
    src: daemon.json.j2
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: "0644"
  notify: Restart docker

- name: Ensure docker service enabled
  ansible.builtin.service:
    name: docker
    enabled: true
    state: started

- name: Flush handlers to restart docker before starting containers
  ansible.builtin.meta: flush_handlers

- name: Ensure isolated admin Docker networks exist
  community.docker.docker_network:
    name: "{{ item.name }}"
    internal: "{{ item.internal }}"
    driver_options:
      com.docker.network.bridge.name: "{{ item.bridge }}"
    state: present
  loop:
    - { name: admin-edge, bridge: br-admin-edge, internal: false }
    - { name: gitea-db, bridge: br-gitea-db, internal: true }
    - { name: keycloak-db, bridge: br-keycloak-db, internal: true }
    - { name: traefik-openbao, bridge: br-traefik-bao, internal: true }
    - { name: openbao-metrics, bridge: br-bao-metrics, internal: true }
    - { name: harbor-internal, bridge: br-harbor, internal: true }
    - { name: harbor-egress, bridge: br-hbr-egress, internal: false }
    - { name: harbor-host, bridge: br-hbr-host, internal: false }

- name: Make the Docker bridge probe image available
  when: admin_mode not in ['restore', 'restore_failed']
  block:
    - name: Pull the pinned Docker bridge probe image
      community.docker.docker_image_pull:
        name: "{{ docker_bridge_probe_image }}"
        pull: not_present
      register: docker_bridge_probe_image_pull
      until: docker_bridge_probe_image_pull is succeeded
      retries: "{{ docker_bridge_probe_pull_retries }}"
      delay: "{{ docker_bridge_probe_pull_delay }}"
  rescue:
    - name: Report an unavailable Docker bridge probe image
      ansible.builtin.fail:
        msg: |
          The pinned Docker bridge probe image could not be downloaded after
          {{ docker_bridge_probe_pull_retries }} attempts.
          Image: {{ docker_bridge_probe_image }}
          Verify registry availability before retrying convergence.

- name: Probe Docker bridge networking support without registry access
  ansible.builtin.command:
    argv:
      - docker
      - run
      - --rm
      - --pull=never
      - --network
      - admin-edge
      - "{{ docker_bridge_probe_image }}"
      - "true"
  register: docker_bridge_probe
  failed_when: false
  changed_when: false
  when: admin_mode not in ['restore', 'restore_failed']

- name: Record Docker bridge support
  ansible.builtin.set_fact:
    docker_bridge_supported: "{{ docker_bridge_probe.rc == 0 }}"
  when: admin_mode not in ['restore', 'restore_failed']

- name: Fail when Docker bridge networking is unavailable
  ansible.builtin.fail:
    msg: |
      Docker bridge networking is unavailable in this environment.
      The admin stack requires bridge-capable container networking
      to start traefik, openbao, keycloak, harbor, and cloudflared.
      The pinned Alpine network probe failed on admin-edge.
      Probe exit code: {{ docker_bridge_probe.rc }}
      Probe stderr: {{ docker_bridge_probe.stderr | default('') | trim }}
  when:
    - admin_mode not in ['restore', 'restore_failed']
    - not docker_bridge_supported
